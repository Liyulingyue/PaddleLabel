# PaddleLabel Frontend V2

A Vite + React 18 + TypeScript rewrite of the PaddleLabel annotation tool.

## Setup

```bash
npm install
npm run dev
```

## API Proxy

Vite proxies `/api` requests to `http://localhost:18000` (FastAPI backend).
